version: "3"
services:
    app:
        image: stevenhvtran/kv-app
        ports:
            - "5000:5000"
        volumes:
            - "./app.py:/usr/src/app/app.py"
        environment:
            - "FLASK_APP=app.py"
            - "FLASK_ENV=development"
